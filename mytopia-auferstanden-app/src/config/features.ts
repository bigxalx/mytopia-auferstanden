/**
 * Global Feature Flags for Mytopia Auferstanden.
 * Used to toggle UI components and experimental features.
 */
export const FEATURES = {
  /**
   * Enable native iOS 18+ bottom accessory with liquid glass effect.
   * This uses the high-performance native UITabBar with glass styling.
   */
  ENABLE_NATIVE_BOTTOM_ACCESSORY: true,

  /**
   * Show the persistent bottom bar for active missions.
   * Testing: Try the UI without it to see if it's strictly necessary.
   */
  SHOW_ACTIVE_MISSION_BAR: false,
};
